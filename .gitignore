# Upstream VocalPy source (its own repository; install via `pip install vocalpy`)
/vocalpy/

# Python build / cache artifacts
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.eggs/

# App output
vocalpy_output/
*.npz
scripts/calls_meta.csv
AGENT_MEMORY_MiMic.md
scripts/lgdel_detection_batch.log
scripts/lgdel_detection_batch.err.log
scripts/lgdel_detection_batch.pid
scripts/lgdel_detection_summary.csv
lgdel_usv_analysis/

# Environments / editors
.venv/
.env
.vscode/
.idea/
.DS_Store

# Analysis outputs (regenerated by the attr_*/attr5_* pipelines)
lgdel_usv_analysis_matched_5min/

# Nested standalone repository, not a submodule of MiMic
figure-viewer/
